$("#organisationType").html($("#organisationType option").sort(function (a, b) 
		{
			return a.text == b.text ? 0 : a.text < b.text ? -1 : 1
		}	
	))
  
  //need to change #organisationType based on your id
